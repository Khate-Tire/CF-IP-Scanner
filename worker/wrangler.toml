name = "cf-ip-scanner-db-proxy"
main = "src/index.js"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

[[hyperdrive]]
binding = "HYPERDRIVE"
id = "YOUR_HYPERDRIVE_ID"

# API_KEY is set via `wrangler secret put API_KEY` — do NOT put secrets in [vars]
# [vars]
# API_KEY = "set-via-wrangler-secret"
